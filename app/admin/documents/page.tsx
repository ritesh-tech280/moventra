"use client";
import {useCallback,useEffect,useState}from"react";import{adminApi}from"@/lib/admin-api";import{ChevronLeft,ChevronRight,RefreshCw}from"lucide-react";
export default function Documents(){const[data,setData]=useState<any[]>([]);const[page,setPage]=useState(1);const[pages,setPages]=useState(0);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);try{const r=await adminApi<any>(`/documents?page=${page}&limit=20`);setData(r.documents);setPages(r.pagination.pages);setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to load documents.")}finally{setLoading(false)}},[page]);useEffect(()=>{load()},[load]);return <div><h1 className="text-2xl font-bold">Documents</h1><p className="mt-1 text-sm text-slate-500">Driver submitted documents and review status.</p><div className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{error?<div className="p-10 text-center text-red-700">Unable to load documents. {error}<button onClick={load} className="ml-2 underline">Retry</button></div>:loading?<div className="animate-pulse p-10 text-slate-400">Loading documents…</div>:data.length===0?<div className="p-12 text-center text-sm text-slate-500">No documents submitted.</div>:<><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Document","Driver","Status","Uploaded","Expiry","Reason"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{data.map(d=><tr key={d._id}><td className="px-4 py-3 font-medium">{d.documentType?.replaceAll("_"," ")}</td><td className="px-4 py-3">{d.driverId?.name||"—"}</td><td className="px-4 py-3 capitalize">{d.verificationStatus}</td><td className="px-4 py-3">{new Date(d.createdAt).toLocaleDateString()}</td><td className="px-4 py-3">{d.expiryDate?new Date(d.expiryDate).toLocaleDateString():"—"}</td><td className="px-4 py-3 text-slate-500">{d.rejectionReason||"—"}</td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t p-3 text-sm text-slate-500"><button onClick={load} className="flex items-center gap-1"><RefreshCw size={14}/>Refresh</button><div className="flex items-center gap-2"><span>Page {page} / {Math.max(pages,1)}</span><button disabled={page<=1} onClick={()=>setPage(page-1)} className="rounded border p-1 disabled:opacity-40"><ChevronLeft size={16}/></button><button disabled={page>=pages} onClick={()=>setPage(page+1)} className="rounded border p-1 disabled:opacity-40"><ChevronRight size={16}/></button></div></div></>}</div></div>}
