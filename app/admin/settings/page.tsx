"use client";
import{useEffect,useState}from"react";import{adminApi}from"@/lib/admin-api";import{ShieldCheck}from"lucide-react";
export default function Settings(){const[admin,setAdmin]=useState<any>(null);const[error,setError]=useState("");useEffect(()=>{adminApi<any>("/me").then(d=>setAdmin(d.admin)).catch(e=>setError(e.message))},[]);return <div><h1 className="text-2xl font-bold">Settings</h1><p className="mt-1 text-sm text-slate-500">Administrator session information.</p><section className="mt-6 max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-5 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><ShieldCheck/></span><div><h2 className="font-semibold">Administrator account</h2><p className="text-xs text-slate-500">Managed through your existing admin account.</p></div></div>{error?<p className="text-sm text-red-700">{error}</p>:!admin?<p className="animate-pulse text-sm text-slate-400">Loading account…</p>:<div className="grid gap-4 sm:grid-cols-2"><div><p className="text-xs text-slate-500">Name</p><p className="mt-1 font-medium">{admin.name}</p></div><div><p className="text-xs text-slate-500">Email</p><p className="mt-1 font-medium">{admin.email}</p></div><div><p className="text-xs text-slate-500">Role</p><p className="mt-1 font-medium capitalize">Administrator</p></div></div>}</section></div>}
